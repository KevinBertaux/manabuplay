# Monorepo : build à la racine (shared + apps/web). Le script build:web est dans package.json racine.
[build]
  base = "."
  command = "npm ci && npm run build:web"
  publish = "dist/web"

[build.environment]
  NODE_VERSION = "22.13.0"
  NPM_CONFIG_PRODUCTION = "false"
