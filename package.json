{
  "name": "manabuplay",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:front": "vite",
    "dev:api": "cd backend && php artisan serve --host=127.0.0.1 --port=8000",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:api": "cd backend && php artisan test",
    "migrate:api": "cd backend && php artisan migrate:fresh --seed",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "pinia": "^2.1.7",
    "vue": "^3.4.0",
    "vue-router": "^4.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitejs/plugin-vue": "^5.0.4",
    "vite": "^5.2.0",
    "vitest": "^2.1.8"
  }
}
