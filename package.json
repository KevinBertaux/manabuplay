{
  "name": "manabuplay",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:px-values": "node scripts/check-px-values.mjs",
    "check:ui": "npm run check:px-values && npm run build && playwright test tests/home-layout.spec.ts --workers=1 && playwright test tests/brand-system.spec.ts --workers=1 && playwright test tests/fx-lab.spec.ts --workers=1",
    "test:brand-system": "npm run build && playwright test tests/brand-system.spec.ts",
    "test:fx-lab": "npm run build && playwright test tests/fx-lab.spec.ts",
    "test:fx-lab-iso": "npm run build && node scripts/compare-fx-lab-iso.mjs",
    "test:layout": "npm run build && playwright test tests/home-layout.spec.ts --workers=1 && playwright test tests/brand-system.spec.ts --workers=1",
    "test:legacy-parity": "npm run build && playwright test tests/visual-compare.spec.ts",
    "test:visual": "npm run build && playwright test",
    "test:visual:headed": "npm run build && playwright test --headed",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.1.2",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.0",
    "@types/pngjs": "^6.0.5",
    "autoprefixer": "^10.4.21",
    "pixelmatch": "^7.1.0",
    "postcss": "^8.5.6",
    "pngjs": "^7.0.0",
    "typescript": "^5.9.3"
  }
}
